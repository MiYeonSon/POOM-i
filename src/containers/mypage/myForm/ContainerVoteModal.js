import React, {useEffect} from 'react';
import {useSelector} from "react-redux";
import {getPlaceInfo} from "../../../lib/api/mypage/spaceVote";

const ContainerVoteModal = ({voteId}) => {




    return (
        <div>
            
        </div>
    );
};

export default ContainerVoteModal;
